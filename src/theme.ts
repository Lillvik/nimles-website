const theme = {
  name: "default",
  colors: {
    standard: "#555",
    primary: "#1f5b98",
    secondary: "#f44336"
  },
  borderRadius: '4px'
};

export type Theme = typeof theme;

export default theme;
